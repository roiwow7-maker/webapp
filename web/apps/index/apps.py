from django.apps import AppConfig

class IndexConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.index"              
    verbose_name = "Catálogo y Modelos 3D"
